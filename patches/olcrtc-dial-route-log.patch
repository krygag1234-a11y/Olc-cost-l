diff --git a/internal/server/server.go b/internal/server/server.go
--- a/internal/server/server.go
+++ b/internal/server/server.go
@@ -877,7 +877,12 @@ func (s *Server) dispatch(stream *smux.Stream, req ConnectRequest, sessionID st
 	}
 	defer func() { _ = conn.Close() }()
 
-	logger.Infof("sid=%d connected %s in %v", stream.ID(), addr, dialElapsed)
+	route := "direct"
+	if s.socksProxyAddr != "" && !s.shouldDialDirect(req.Addr) {
+		route = "tor"
+	}
+	logger.Infof("sid=%d connect %s route=%s in %v", stream.ID(), addr, route, dialElapsed)
 
 	if _, err := stream.Write([]byte{0x00}); err != nil {
 		return
